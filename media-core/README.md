# media-core (C++)

Realtime SIP/media core built on pjsip/pjnath/libsrtp with gRPC over UDS control plane.

This is a skeleton. Build steps will be added after integrating dependencies (CMake + vcpkg/conan).